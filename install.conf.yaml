- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''

- shell:
  # the following have to run after linking ~/.gitconfig
  - git submodule update --init --recursive --depth 1
