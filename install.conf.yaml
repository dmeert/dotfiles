- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.zshrc:

- shell:
  # the following have to run after linking ~/.gitconfig
  - [git submodule update --init --recursive --depth 1, Checking submodules]
